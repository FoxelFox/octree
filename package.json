{
  "type": "module",
  "workspaces": [
    "src/my-lib/pkg"
  ],
  "scripts": {
    "build": "wasm-pack --quiet build src/my-lib --target web && bun run build.ts",
    "dev": "wasm-pack --quiet build src/my-lib --target web && bun run build.ts --dev",
    "start": "http-server dist -c-1",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@webgpu/types": "^0.1.64",
    "http-server": "^14.0.0"
  },
  "dependencies": {
    "wgpu-matrix": "^3.4.0"
  }
}
